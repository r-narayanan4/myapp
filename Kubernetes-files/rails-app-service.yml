apiVersion: v1
kind: Service
metadata:
  name: myruby-service
  labels:
    app: myruby           # Selector for the Service
spec:
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: myruby           # Selector for the Service

